// Viewport size, probably don't want to touch these
ViewportWidth=1920
ViewportHeight=1080

// This is the area available for Glyphs to position and scale themselves
GlyphAreaWidth=850
GlyphAreaHeight=630

// This is the offset from the monitor background image at which Glyph Area starts
GlyphAreaMonitorOffsetX=180
GlyphAreaMonitorOffsetY=223

// Glyphs MUST BE square, this is the maximum dimension that the glyph can have when rendering
GlyphMaxEdgeRenderSize=256

// This is the edge size of the actual glyph in a file
GlyphImageEdge=256